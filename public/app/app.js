const tg = window.Telegram?.WebApp;
if (tg) {
  tg.ready();
  tg.expand();
  try { tg.setHeaderColor('#fcfbf8'); } catch {}
  try { tg.setBackgroundColor('#fcfbf8'); } catch {}
}

const root = document.getElementById('app');
const viewRoot = document.getElementById('viewRoot');
const bottomNav = document.getElementById('bottomNav');
const toastRegion = document.getElementById('toastRegion');
const sheetRoot = document.getElementById('sheetRoot');
const filePicker = document.getElementById('novelFilePicker');
const previewBanner = document.getElementById('previewBanner');

const state = {
  bootstrap: null,
  ui: {},
  locale: 'en',
  view: 'home',
  previousView: null,
  queueSegment: 'active',
  queueLanguage: 'all',
  requestFilter: 'all',
  adminKind: 'pending',
  adminData: null,
  file: null,
  fileAnalysis: null,
  wizardStep: 1,
  draft: {
    title: '', original_language: '', chapter_count: '', publication_status: 'ongoing',
    source_url: '', genres_tags: '', sexual_level: 'none', sexual_content: 'None',
    sensitive_content: 'None', notes: '', rules_accepted: false,
  },
  preview: !(tg?.initData),
};

const BASE_UI = {
  home:'Home',queue:'Queue',suggest:'Suggest',requests:'My Requests',account:'Account',admin:'Admin',greeting:'Good to see you',tagline:"Let’s bring more amazing stories to life.",premiumAccount:'Premium account',regularAccount:'Regular account',boostySubscriber:'Boosty Subscriber',regularStatus:'Regular',thisMonthUsage:"This month's usage",remainingRequests:'Remaining requests',requestsUsed:'requests used',requestsLeft:'requests left',suggestNovel:'Suggest a Novel',currentlyTranslating:'Currently Translating',currentlyTranslatingDesc:"The novel we’re actively working on right now.",viewQueue:'View Queue',myRequests:'My Requests',viewAll:'View All',qualityLine:'Quality translations. Community powered.',translationQueue:'Translation Queue',queueSubtitle:"See what’s being translated and what’s coming next.",all:'All',korean:'Korean',japanese:'Japanese',chinese:'Chinese',upNext:'Up Next',queueEmpty:'No novels are waiting in the queue yet.',noActive:'No novel is currently being translated.',queueAuto:"Queue updates automatically. We’ll notify you when your novel starts.",originalLanguage:'Original Language',totalChapters:'Total Chapters',queuePosition:'Queue Position',status:'Status',lastUpdated:'Last Updated',openOriginal:'Open Original Source',translationActive:'Actively translating',chapters:'chapters',position:'Position',step:'Step',upload:'Upload',details:'Details',tagsContent:'Tags & Content',review:'Review',uploadNovelFile:'Upload your novel file',uploadCopy:'We support .txt and .epub files. Tap to browse.',uploadButton:'Upload TXT or EPUB',filePrivate:'Your file is transferred securely to the Dollar TL bot.',originalSourceOptional:'Original Source (Optional)',sourceHelp:'Add a link to the original novel page or another public source.',autoFillHint:'We’ll auto-fill key details after analysis',autoFillCopy:'Language, chapter count and other details can be suggested to save you time.',continue:'Continue',back:'Back',analyzing:'Analyzing file…',readingFile:'Reading file',detectingLanguage:'Detecting language',findingChapters:'Finding chapters',readingStructure:'Reading structure',fileAnalyzed:'File analyzed successfully',autoDetected:'Auto-detected',detectedDetails:'Detected Novel Details',novelTitle:'Novel Title',chapterCount:'Chapter Count',publicationStatus:'Publication Status',ongoing:'Ongoing',completed:'Completed',chapterLimit:'Chapter Suggestion Limit',regularLimitCopy:'Regular users can suggest novels with up to 250 chapters. Boosty subscribers are not limited by that restriction.',boostyNoLimit:'No 250-chapter restriction',couldNotDetect:'Could not detect reliably',genresTags:'Genres & Tags',genresHelp:'Add the main genres and relevant tags.',addTags:'Add genres or tags…',popularTags:'Popular tags',sexualContent:'Sexual Content / Fetishes',sexualHelp:'Be clear—this helps us review the request correctly.',noSexual:'No sexual content',suggestive:'Suggestive',explicit:'Explicit (18+)',sexualDetails:'Describe sexual content, fetishes or kinks',sensitiveContent:'Sensitive / Controversial Content',sensitiveHelp:'Describe any extreme, disturbing, controversial or sensitive themes.',whyMatters:'Why this matters',contentWhy:'Accurate content notes help us review the novel and keep submissions within Dollar TL rules.',additionalNotes:'Additional Notes (Optional)',notesPlaceholder:'Anything else we should know?',reviewYourRequest:'Review Your Request',reviewCopy:'Please review all details before submitting.',uploadedSource:'Uploaded Source',detectedInfo:'Novel Details',disclosure:'Content Disclosure',afterSubmission:'After submission',rulesAgree:'I confirm that I have read the Dollar TL rules and disclosed important tags, sexual/fetish content and sensitive content honestly.',submitRequest:'Submit Request',submitting:'Submitting…',requestSubmitted:'Request submitted',requestSubmittedCopy:'Your novel request was sent for manual review.',backHome:'Back Home',requestsSubtitle:'Track the status and progress of all your novel requests.',active:'Active',rejected:'Rejected',pending:'Pending',inQueue:'In Queue',inProgress:'In Progress',rejectedReturned:'Rejected · quota returned',requested:'Requested',accountTitle:'Account',accountSubtitle:'Manage your account and preferences.',currentPlan:'Current Plan',preferences:'Preferences',language:'Language',supportResources:'Support & Resources',helpGuide:'Help / Guide',rules:'Rules',openTelegramChat:'Open Telegram Chat',subscription:'Boosty Subscription',adminTitle:'Admin',adminSubtitle:'Manage translation requests and the public queue.',pendingRequests:'Pending Requests',queued:'Queued',adminInProgress:'In Progress',adminCompleted:'Completed',requiresAction:'Requires action',accept:'Accept',reject:'Reject',returnSlot:'Return Slot',start:'Start',complete:'Complete',backToQueue:'Back to Queue',moveUp:'Move Up',moveDown:'Move Down',currentChapter:'Current chapter',update:'Update',openRawInTelegram:'Raw file is already available in the admin Telegram chat.',previewMode:'Preview mode',openFromTelegram:'Open this Mini App from Telegram to use real data.',retry:'Try Again',verificationUnavailable:'Boosty verification is temporarily unavailable.',fileTooLarge:'The file is too large. Maximum Mini App upload size is 45 MB.',unsupportedFile:'Only TXT and EPUB files are supported.',regularTooLong:'This novel is over the 250-chapter limit for Regular users.',quotaReached:'Your monthly request limit has been reached.',genericError:'Something went wrong. Please try again.',save:'Save',close:'Close',confirm:'Confirm',cancel:'Cancel'
};

const UI_OVERRIDES = {
  ru:{home:'Главная',queue:'Очередь',suggest:'Предложить',requests:'Мои заявки',account:'Профиль',admin:'Админ',greeting:'Рады вас видеть',tagline:'Давайте найдём ещё больше отличных историй для перевода.',premiumAccount:'Премиум-статус',regularAccount:'Обычный статус',boostySubscriber:'Подписчик Boosty',regularStatus:'Обычный',thisMonthUsage:'Заявки в этом месяце',remainingRequests:'Осталось заявок',requestsUsed:'использовано',requestsLeft:'осталось',suggestNovel:'Предложить новеллу',currentlyTranslating:'Сейчас переводим',currentlyTranslatingDesc:'Новелла, над которой команда работает сейчас.',viewQueue:'Открыть очередь',myRequests:'Мои заявки',viewAll:'Все',qualityLine:'Качественные переводы. Вместе с сообществом.',translationQueue:'Очередь переводов',queueSubtitle:'Посмотрите, что переводится сейчас и что будет дальше.',all:'Все',korean:'Корейский',japanese:'Японский',chinese:'Китайский',upNext:'Дальше в очереди',queueEmpty:'В очереди пока нет новелл.',noActive:'Сейчас нет активного перевода.',queueAuto:'Очередь обновляется автоматически. Мы уведомим вас, когда начнётся перевод вашей новеллы.',originalLanguage:'Язык оригинала',totalChapters:'Всего глав',queuePosition:'Позиция в очереди',status:'Статус',lastUpdated:'Обновлено',openOriginal:'Открыть оригинал',translationActive:'Перевод идёт',chapters:'глав',position:'Позиция',step:'Шаг',upload:'Файл',details:'Данные',tagsContent:'Теги и контент',review:'Проверка',uploadNovelFile:'Загрузите файл новеллы',uploadCopy:'Поддерживаются .txt и .epub. Нажмите, чтобы выбрать файл.',uploadButton:'Загрузить TXT или EPUB',filePrivate:'Файл безопасно передаётся боту Dollar TL.',originalSourceOptional:'Ссылка на оригинал (необязательно)',sourceHelp:'Добавьте ссылку на страницу оригинальной новеллы или другой публичный источник.',autoFillHint:'После анализа мы заполним часть данных автоматически',autoFillCopy:'Язык, количество глав и другие поля можно определить автоматически.',continue:'Продолжить',back:'Назад',analyzing:'Анализируем файл…',readingFile:'Читаем файл',detectingLanguage:'Определяем язык',findingChapters:'Ищем главы',readingStructure:'Читаем структуру',fileAnalyzed:'Файл успешно проанализирован',autoDetected:'Определено автоматически',detectedDetails:'Данные новеллы',novelTitle:'Название новеллы',chapterCount:'Количество глав',publicationStatus:'Статус публикации',ongoing:'Продолжается',completed:'Завершена',chapterLimit:'Ограничение по главам',regularLimitCopy:'Пользователи с обычным статусом могут предлагать новеллы до 250 глав. На подписчиков Boosty это ограничение не распространяется.',boostyNoLimit:'Ограничение в 250 глав не действует',couldNotDetect:'Не удалось определить надёжно',genresTags:'Жанры и теги',genresHelp:'Укажите основные жанры и важные теги.',addTags:'Добавить жанры или теги…',popularTags:'Популярные теги',sexualContent:'Сексуальный контент / фетиши',sexualHelp:'Укажите всё важное — это нужно для корректной проверки.',noSexual:'Нет сексуального контента',suggestive:'Намеки / неявный',explicit:'Откровенный (18+)',sexualDetails:'Опишите сексуальный контент, фетиши или кинки',sensitiveContent:'Чувствительный / спорный контент',sensitiveHelp:'Опишите экстремальные, тревожные, спорные или чувствительные темы.',whyMatters:'Почему это важно',contentWhy:'Точные предупреждения помогают проверить новеллу и убедиться, что она соответствует правилам Dollar TL.',additionalNotes:'Дополнительные заметки (необязательно)',notesPlaceholder:'Что ещё нам стоит знать?',reviewYourRequest:'Проверьте заявку',reviewCopy:'Проверьте данные перед отправкой.',uploadedSource:'Исходный файл',detectedInfo:'Данные новеллы',disclosure:'Информация о контенте',afterSubmission:'После отправки',rulesAgree:'Я подтверждаю, что прочитал правила Dollar TL и честно указал важные теги, сексуальный/фетиш-контент и чувствительный контент.',submitRequest:'Отправить заявку',submitting:'Отправляем…',requestSubmitted:'Заявка отправлена',requestSubmittedCopy:'Новелла отправлена на ручную проверку.',backHome:'На главную',requestsSubtitle:'Отслеживайте статус и прогресс всех своих заявок.',active:'Активные',rejected:'Отклонённые',pending:'На проверке',inQueue:'В очереди',inProgress:'В работе',rejectedReturned:'Отклонена · лимит возвращён',requested:'Отправлена',accountTitle:'Профиль',accountSubtitle:'Статус, язык и полезные разделы.',currentPlan:'Текущий статус',preferences:'Настройки',language:'Язык',supportResources:'Помощь',helpGuide:'Как это работает',rules:'Правила',openTelegramChat:'Открыть чат с ботом',subscription:'Подписка Boosty',adminTitle:'Админ',adminSubtitle:'Управление заявками и очередью переводов.',pendingRequests:'Новые заявки',queued:'В очереди',adminInProgress:'В работе',adminCompleted:'Завершено',requiresAction:'Требует решения',accept:'Принять',reject:'Отклонить',returnSlot:'Вернуть лимит',start:'Начать',complete:'Завершить',backToQueue:'Вернуть в очередь',moveUp:'Выше',moveDown:'Ниже',currentChapter:'Текущая глава',update:'Обновить',openRawInTelegram:'Raw-файл уже доступен в админском чате Telegram.',previewMode:'Режим предпросмотра',openFromTelegram:'Откройте Mini App из Telegram, чтобы загрузить реальные данные.',retry:'Повторить',verificationUnavailable:'Проверка Boosty временно недоступна.',fileTooLarge:'Файл слишком большой. Максимальный размер в Mini App — 45 МБ.',unsupportedFile:'В Mini App поддерживаются только TXT и EPUB.',regularTooLong:'Новелла превышает лимит 250 глав для обычного статуса.',quotaReached:'Месячный лимит заявок исчерпан.',genericError:'Что-то пошло не так. Попробуйте ещё раз.',save:'Сохранить',close:'Закрыть',confirm:'Подтвердить',cancel:'Отмена'},
  es:{home:'Inicio',queue:'Cola',suggest:'Proponer',requests:'Mis solicitudes',account:'Cuenta',admin:'Admin',suggestNovel:'Proponer una novela',currentlyTranslating:'Traduciendo ahora',viewQueue:'Ver cola',translationQueue:'Cola de traducción',upNext:'A continuación',upload:'Archivo',details:'Detalles',tagsContent:'Etiquetas y contenido',review:'Revisión',continue:'Continuar',back:'Atrás',novelTitle:'Título de la novela',chapterCount:'Número de capítulos',publicationStatus:'Estado de publicación',ongoing:'En publicación',completed:'Completada',pending:'Pendiente',inQueue:'En cola',inProgress:'En curso',rejected:'Rechazada',accept:'Aceptar',reject:'Rechazar',returnSlot:'Devolver cupo'},
  fil:{home:'Home',queue:'Pila',suggest:'Magmungkahi',requests:'Mga Kahilingan Ko',account:'Account',admin:'Admin',suggestNovel:'Magmungkahi ng Nobela',currentlyTranslating:'Kasalukuyang Isinasalin',viewQueue:'Tingnan ang Pila',translationQueue:'Pila ng mga Isasalin',upNext:'Susunod',upload:'File',details:'Detalye',tagsContent:'Mga Tag at Nilalaman',review:'Suriin',continue:'Magpatuloy',back:'Bumalik',novelTitle:'Pamagat ng Nobela',chapterCount:'Bilang ng Kabanata',publicationStatus:'Kalagayan ng Publikasyon',ongoing:'Patuloy',completed:'Kumpleto',pending:'Sinusuri',inQueue:'Nasa Pila',inProgress:'Isinasalin',rejected:'Tinanggihan',accept:'Tanggapin',reject:'Tanggihan',returnSlot:'Ibalik ang quota'},
  hi:{home:'मुख्य',queue:'कतार',suggest:'उपन्यास सुझाएँ',requests:'मेरे अनुरोध',account:'खाता',admin:'एडमिन',suggestNovel:'उपन्यास सुझाएँ',currentlyTranslating:'अभी अनुवाद जारी',viewQueue:'कतार देखें',translationQueue:'अनुवाद कतार',upNext:'आगे',upload:'फ़ाइल',details:'जानकारी',tagsContent:'टैग और सामग्री',review:'जाँच',continue:'आगे बढ़ें',back:'वापस',novelTitle:'उपन्यास का नाम',chapterCount:'अध्यायों की संख्या',publicationStatus:'प्रकाशन स्थिति',ongoing:'जारी',completed:'पूर्ण',pending:'समीक्षा बाकी',inQueue:'कतार में',inProgress:'अनुवाद जारी',rejected:'अस्वीकृत',accept:'स्वीकार करें',reject:'अस्वीकार करें',returnSlot:'सीमा वापस करें'},
  pt:{home:'Início',queue:'Fila',suggest:'Sugerir',requests:'Meus pedidos',account:'Conta',admin:'Admin',suggestNovel:'Sugerir uma Novel',currentlyTranslating:'Em tradução agora',viewQueue:'Ver fila',translationQueue:'Fila de tradução',upNext:'A seguir',upload:'Arquivo',details:'Detalhes',tagsContent:'Tags e conteúdo',review:'Revisão',continue:'Continuar',back:'Voltar',novelTitle:'Título da novel',chapterCount:'Número de capítulos',publicationStatus:'Status de publicação',ongoing:'Em andamento',completed:'Concluída',pending:'Pendente',inQueue:'Na fila',inProgress:'Em andamento',rejected:'Rejeitado',accept:'Aceitar',reject:'Rejeitar',returnSlot:'Devolver cota'},
  id:{home:'Beranda',queue:'Antrean',suggest:'Ajukan',requests:'Permintaan Saya',account:'Akun',admin:'Admin',suggestNovel:'Ajukan Novel',currentlyTranslating:'Sedang Diterjemahkan',viewQueue:'Lihat Antrean',translationQueue:'Antrean Terjemahan',upNext:'Berikutnya',upload:'Berkas',details:'Detail',tagsContent:'Tag & Konten',review:'Tinjau',continue:'Lanjutkan',back:'Kembali',novelTitle:'Judul novel',chapterCount:'Jumlah bab',publicationStatus:'Status publikasi',ongoing:'Berjalan',completed:'Selesai',pending:'Menunggu tinjauan',inQueue:'Dalam antrean',inProgress:'Sedang diterjemahkan',rejected:'Ditolak',accept:'Terima',reject:'Tolak',returnSlot:'Kembalikan kuota'},
  vi:{home:'Trang chủ',queue:'Hàng đợi',suggest:'Đề xuất',requests:'Đề xuất của tôi',account:'Tài khoản',admin:'Quản trị',suggestNovel:'Đề xuất tiểu thuyết',currentlyTranslating:'Đang dịch',viewQueue:'Xem hàng đợi',translationQueue:'Hàng đợi dịch',upNext:'Tiếp theo',upload:'Tệp',details:'Thông tin',tagsContent:'Thẻ & Nội dung',review:'Kiểm tra',continue:'Tiếp tục',back:'Quay lại',novelTitle:'Tên tiểu thuyết',chapterCount:'Số chương',publicationStatus:'Trạng thái xuất bản',ongoing:'Đang phát hành',completed:'Đã hoàn thành',pending:'Chờ duyệt',inQueue:'Trong hàng đợi',inProgress:'Đang dịch',rejected:'Bị từ chối',accept:'Chấp nhận',reject:'Từ chối',returnSlot:'Hoàn lượt gửi'},
  fr:{home:'Accueil',queue:'File',suggest:'Proposer',requests:'Mes demandes',account:'Compte',admin:'Admin',suggestNovel:'Proposer un roman',currentlyTranslating:'En cours de traduction',viewQueue:'Voir la file',translationQueue:'File de traduction',upNext:'À suivre',upload:'Fichier',details:'Détails',tagsContent:'Tags et contenu',review:'Vérification',continue:'Continuer',back:'Retour',novelTitle:'Titre du roman',chapterCount:'Nombre de chapitres',publicationStatus:'Statut de publication',ongoing:'En cours',completed:'Terminé',pending:'En attente',inQueue:'Dans la file',inProgress:'En cours',rejected:'Refusée',accept:'Accepter',reject:'Refuser',returnSlot:'Rendre le quota'},
  de:{home:'Start',queue:'Warteschlange',suggest:'Vorschlagen',requests:'Meine Anfragen',account:'Konto',admin:'Admin',suggestNovel:'Roman vorschlagen',currentlyTranslating:'Aktuell in Übersetzung',viewQueue:'Warteschlange öffnen',translationQueue:'Übersetzungswarteschlange',upNext:'Als Nächstes',upload:'Datei',details:'Details',tagsContent:'Tags & Inhalte',review:'Prüfen',continue:'Weiter',back:'Zurück',novelTitle:'Romantitel',chapterCount:'Kapitelzahl',publicationStatus:'Veröffentlichungsstatus',ongoing:'Laufend',completed:'Abgeschlossen',pending:'Wird geprüft',inQueue:'In der Warteschlange',inProgress:'In Arbeit',rejected:'Abgelehnt',accept:'Annehmen',reject:'Ablehnen',returnSlot:'Kontingent zurückgeben'}
};

const LANGUAGE_NAMES = { en:'English', es:'Español', fil:'Filipino', hi:'हिन्दी', pt:'Português', id:'Bahasa Indonesia', vi:'Tiếng Việt', fr:'Français', de:'Deutsch', ru:'Русский' };
const POPULAR_TAGS = ['Fantasy','Romance','Adventure','Academy','Isekai','Reincarnation','Magic','Strong MC','Harem','Slice of Life','Time Travel','System','Villainess','Slow Burn'];

function tr(key) { return state.ui[key] ?? BASE_UI[key] ?? key; }
function escapeHtml(value='') { return String(value).replace(/[&<>'"]/g, ch => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[ch])); }
function languageFlag(value='') { const v=value.toLowerCase(); if(v.includes('korean')||v.includes('корей')||v.includes('한국'))return'🇰🇷'; if(v.includes('japanese')||v.includes('япон')||v.includes('日本'))return'🇯🇵'; if(v.includes('chinese')||v.includes('китай')||v.includes('中文'))return'🇨🇳'; if(v.includes('english')||v.includes('англ'))return'🇬🇧'; return'🌐'; }
function initials(title='DTL') { return title.split(/\s+/).filter(Boolean).slice(0,2).map(x=>x[0]).join('').toUpperCase(); }
function cover(title, small=false) { return `<div class="novel-cover${small?' small':''}" aria-hidden="true">${escapeHtml(initials(title))}</div>`; }
function formatDate(value) { if(!value)return'—'; try{return new Intl.DateTimeFormat(state.locale,{dateStyle:'medium'}).format(new Date(value));}catch{return value;} }
function relativeTime(value) { if(!value)return'—'; const ms=Date.now()-new Date(value).getTime(); if(ms<0)return formatDate(value); const min=Math.floor(ms/60000); if(min<1)return'just now'; if(min<60)return`${min} min ago`; const h=Math.floor(min/60); if(h<24)return`${h} h ago`; const d=Math.floor(h/24); if(d<8)return`${d} d ago`; return formatDate(value); }

async function api(path, options={}) {
  if (state.preview) throw new Error('preview');
  const headers = new Headers(options.headers || {});
  headers.set('x-telegram-init-data', tg.initData);
  const response = await fetch(path, { ...options, headers });
  const data = await response.json().catch(()=>({}));
  if (!response.ok) {
    const error = new Error(data?.error?.message || tr('genericError'));
    error.code = data?.error?.code;
    error.status = response.status;
    throw error;
  }
  return data;
}

function previewBootstrap() {
  return {
    user:{id:1,first_name:'Alex',username:'alex',locale:'en',is_admin:true},
    account:{plan:'subscriber',verification_error:false,used:1,limit:5,remaining:4,regular_max_chapters:250,boosty_url:'https://boosty.to/domnekromanta'},
    queue:{active:[{id:1,title:'The Pure Love of the Humiliation Academy',original_language:'Korean',chapter_count:258,queue_status:'in_progress',queue_position:1,current_chapter:78,progress_percent:30,progress_updated_at:new Date(Date.now()-7200000).toISOString(),updated_at:new Date().toISOString()}],upcoming:[{id:2,title:'Pokémon Master of Tactics',original_language:'Korean',chapter_count:196,queue_status:'queued',queue_position:2,progress_percent:null,updated_at:new Date().toISOString()},{id:3,title:'Reborn as the Villainess’ Guard Dog',original_language:'Japanese',chapter_count:96,queue_status:'queued',queue_position:3,progress_percent:null,updated_at:new Date().toISOString()},{id:4,title:'The Saint’s Secret Wedding',original_language:'Korean',chapter_count:112,queue_status:'queued',queue_position:4,progress_percent:null,updated_at:new Date().toISOString()}],completed:[]},
    my_requests:[{id:2,title:'Pokémon Master of Tactics',original_language:'Korean',chapter_count:196,status:'accepted',slot_returned:0,queue_status:'queued',queue_position:2,state:'queued',created_at:new Date(Date.now()-86400000).toISOString()},{id:5,title:'The Saint’s Secret Wedding',original_language:'Korean',chapter_count:120,status:'accepted',slot_returned:0,queue_status:'in_progress',queue_position:5,current_chapter:78,progress_percent:65,state:'in_progress',created_at:new Date(Date.now()-4*86400000).toISOString()}],
    admin:{pending:3,queued:8,in_progress:1,completed:56,total:68}
  };
}

async function init() {
  if (state.preview) {
    state.bootstrap = previewBootstrap();
    previewBanner.hidden = false;
  } else {
    try { state.bootstrap = await api('/api/app/bootstrap'); }
    catch (error) { renderFatal(error); return; }
  }
  state.locale = state.bootstrap.user.locale || 'en';
  state.ui = { ...BASE_UI, ...(UI_OVERRIDES[state.locale] || {}) };
  root.setAttribute('aria-busy','false');
  renderNav();
  navigate('home', false);
}

function renderFatal(error) {
  root.setAttribute('aria-busy','false');
  viewRoot.innerHTML = `<section class="page"><div class="empty-state surface-card"><div class="empty-icon">!</div><h2>${escapeHtml(tr('openFromTelegram'))}</h2><p>${escapeHtml(error?.message || tr('genericError'))}</p><button class="primary-button wide-button" id="fatalRetry" type="button" style="margin-top:18px">${escapeHtml(tr('retry'))}</button></div></section>`;
  document.getElementById('fatalRetry')?.addEventListener('click',()=>location.reload());
}

function navItems() {
  return state.bootstrap?.user?.is_admin
    ? [['home','⌂'],['queue','▱'],['requests','▤'],['account','♙'],['admin','⚙']]
    : [['home','⌂'],['queue','▱'],['suggest','＋'],['requests','▤'],['account','♙']];
}
function renderNav() {
  bottomNav.innerHTML = navItems().map(([id,icon])=>`<button class="nav-item${state.view===id?' active':''}" type="button" data-nav="${id}"><span class="nav-icon">${icon}</span><span>${escapeHtml(tr(id))}</span></button>`).join('');
  bottomNav.querySelectorAll('[data-nav]').forEach(btn=>btn.addEventListener('click',()=>navigate(btn.dataset.nav)));
}
function navigate(view, push=true) {
  if (push && state.view !== view) state.previousView = state.view;
  state.view = view;
  renderNav();
  renderView();
  viewRoot.focus({preventScroll:true});
  window.scrollTo({top:0,behavior:window.matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth'});
  try { tg?.HapticFeedback?.selectionChanged(); } catch {}
}
function renderView() {
  if (state.view==='home') return renderHome();
  if (state.view==='queue') return renderQueue();
  if (state.view==='suggest') return renderSuggest();
  if (state.view==='requests') return renderRequests();
  if (state.view==='account') return renderAccount();
  if (state.view==='admin') return renderAdmin();
}

function renderHome() {
  const b=state.bootstrap, a=b.account, active=b.queue.active?.[0], mine=b.my_requests||[];
  const name=escapeHtml(b.user.first_name || b.user.username || 'Reader');
  viewRoot.innerHTML=`<section class="page stagger">
    <div class="page-heading"><h1>${escapeHtml(tr('greeting'))}, ${name} 👋</h1><p class="subtitle">${escapeHtml(tr('tagline'))}</p></div>
    ${accountCard(a)}
    <div class="cta-wrap"><button class="primary-button wide-button" type="button" id="homeSuggest">▱ ${escapeHtml(tr('suggestNovel'))} <span style="margin-left:auto">›</span></button></div>
    <div class="small muted" style="text-align:center;margin-top:10px">◇ ${escapeHtml(tr('qualityLine'))}</div>
    <section class="section"><div class="section-header"><h2>${escapeHtml(tr('currentlyTranslating'))}</h2><button class="link-button" id="homeQueue" type="button">${escapeHtml(tr('viewQueue'))} ›</button></div>${active?featuredNovel(active):emptyCard('⚡',tr('noActive'))}</section>
    <section class="section"><div class="section-header"><h2>${escapeHtml(tr('myRequests'))}</h2><button class="link-button" id="homeRequests" type="button">${escapeHtml(tr('viewAll'))} ›</button></div>${mine.length?`<div class="surface-card simple-list">${mine.slice(0,3).map(requestListRow).join('')}</div>`:emptyCard('▤','No requests yet.')}</section>
  </section>`;
  document.getElementById('homeSuggest')?.addEventListener('click',()=>navigate('suggest'));
  document.getElementById('homeQueue')?.addEventListener('click',()=>navigate('queue'));
  document.getElementById('homeRequests')?.addEventListener('click',()=>navigate('requests'));
  bindNovelLinks();
}

function accountCard(a) {
  const subscriber=a.plan==='subscriber';
  return `<div class="premium-card"><div class="premium-top"><div class="premium-emblem">${subscriber?'♛':'◇'}</div><div><div class="premium-label">${escapeHtml(tr(subscriber?'premiumAccount':'regularAccount'))}</div><div class="premium-name">${escapeHtml(tr(subscriber?'boostySubscriber':'regularStatus'))}</div><div class="premium-note">${subscriber?'Thank you for supporting novel translations!':`Up to ${a.regular_max_chapters} chapters per novel.`}</div></div><div class="plan-check">✓</div></div><div class="usage-box"><div class="usage-item"><div class="round-icon">▣</div><div><div class="usage-label">${escapeHtml(tr('thisMonthUsage'))}</div><div class="usage-value">${a.used} / ${a.limit}</div><div class="usage-caption">${escapeHtml(tr('requestsUsed'))}</div></div></div><div class="usage-divider"></div><div class="usage-item"><div class="round-icon">✦</div><div><div class="usage-label">${escapeHtml(tr('remainingRequests'))}</div><div class="usage-value">${a.remaining}</div><div class="usage-caption">${escapeHtml(tr('requestsLeft'))}</div></div></div></div>${a.verification_error?`<div class="tip-card warn" style="margin-top:12px"><div class="round-icon">!</div><div><div class="tip-title">${escapeHtml(tr('verificationUnavailable'))}</div></div></div>`:''}</div>`;
}

function featuredNovel(row) {
  const progress=Number.isFinite(row.progress_percent)?row.progress_percent:null;
  return `<button class="surface-card novel-card" type="button" data-novel="${row.id}" style="width:100%;text-align:left;border-style:solid"><div>${cover(row.title)}</div><div class="novel-card-main"><div class="novel-title">${escapeHtml(row.title)}</div><div class="novel-meta"><span>${languageFlag(row.original_language)} ${escapeHtml(row.original_language)}</span><span>→</span><span>English</span></div>${progress!==null?`<div class="progress-block"><div class="progress-labels"><span>Chapter Progress</span><strong>${row.current_chapter} / ${row.chapter_count}</strong></div><div class="progress-track"><div class="progress-fill" style="width:${progress}%"></div></div></div>`:''}<div class="novel-status-row"><span class="status-pill green"><span class="status-dot"></span>${escapeHtml(tr('active'))}</span><span class="updated-text">${escapeHtml(relativeTime(row.progress_updated_at||row.updated_at))}</span></div></div></button>`;
}
function requestListRow(row) { return `<button class="list-row" type="button" data-novel="${row.id}">${cover(row.title,true)}<span class="list-copy"><span class="list-title">${escapeHtml(row.title)}</span><span class="list-meta">${languageFlag(row.original_language)} ${escapeHtml(row.original_language)} · ${row.chapter_count} ${escapeHtml(tr('chapters'))}</span></span>${statusPill(row.state)}</button>`; }
function emptyCard(icon,text) { return `<div class="surface-card empty-state"><div class="empty-icon">${icon}</div><p>${escapeHtml(text)}</p></div>`; }
function statusPill(stateName) { const map={pending:['orange','pending'],queued:['blue','inQueue'],in_progress:['green','inProgress'],completed:['green','completed'],rejected:['red','rejected'],rejected_returned:['red','rejectedReturned']}; const [color,key]=map[stateName]||['gold','pending']; return `<span class="status-pill ${color}">${escapeHtml(tr(key))}</span>`; }
function bindNovelLinks() { document.querySelectorAll('[data-novel]').forEach(el=>el.addEventListener('click',()=>openNovel(Number(el.dataset.novel)))); }

function renderQueue() {
  const q=state.bootstrap.queue;
  const active=q.active||[], upcoming=(q.upcoming||[]).filter(row=>matchesLanguage(row,state.queueLanguage));
  viewRoot.innerHTML=`<section class="page"><div class="page-heading"><h1>${escapeHtml(tr('translationQueue'))}</h1><p class="subtitle">${escapeHtml(tr('queueSubtitle'))}</p></div>
  <div class="filter-row">${[['all',tr('all')],['korean',tr('korean')],['japanese',tr('japanese')],['chinese',tr('chinese')]].map(([id,label])=>`<button class="filter-chip${state.queueLanguage===id?' active':''}" type="button" data-q-lang="${id}">${escapeHtml(label)}</button>`).join('')}</div>
  <div class="segmented"><button class="${state.queueSegment==='active'?'active':''}" type="button" data-q-segment="active">⚡ ${escapeHtml(tr('currentlyTranslating'))}</button><button class="${state.queueSegment==='upcoming'?'active':''}" type="button" data-q-segment="upcoming">◷ ${escapeHtml(tr('upNext'))}</button></div>
  ${state.queueSegment==='active'?renderActiveQueue(active):renderUpcomingQueue(upcoming)}
  <div class="small muted" style="text-align:center;margin:22px 10px 0">◇ ${escapeHtml(tr('queueAuto'))}</div></section>`;
  document.querySelectorAll('[data-q-lang]').forEach(btn=>btn.addEventListener('click',()=>{state.queueLanguage=btn.dataset.qLang;renderQueue();}));
  document.querySelectorAll('[data-q-segment]').forEach(btn=>btn.addEventListener('click',()=>{state.queueSegment=btn.dataset.qSegment;renderQueue();}));
  bindNovelLinks();
}
function matchesLanguage(row,filter){if(filter==='all')return true;return row.original_language?.toLowerCase().includes(filter);}
function renderActiveQueue(rows){if(!rows.length)return emptyCard('⚡',tr('noActive'));return `<section class="section" style="margin-top:0"><div class="section-header"><div><h2>⚡ ${escapeHtml(tr('currentlyTranslating'))}</h2><p class="subtitle" style="margin-top:4px">${escapeHtml(tr('currentlyTranslatingDesc'))}</p></div></div><div class="stagger">${rows.map(featuredNovel).join('')}</div></section>`;}
function renderUpcomingQueue(rows){if(!rows.length)return emptyCard('◷',tr('queueEmpty'));return `<section class="section" style="margin-top:0"><div class="section-header"><h2>◷ ${escapeHtml(tr('upNext'))}</h2></div><div class="surface-card simple-list">${rows.map((r,i)=>`<button class="list-row queue-row" type="button" data-novel="${r.id}"><span class="queue-number">${r.queue_position??i+1}</span>${cover(r.title,true)}<span class="list-copy"><span class="list-title">${escapeHtml(r.title)}</span><span class="list-meta">${languageFlag(r.original_language)} ${escapeHtml(r.original_language)} · ${r.chapter_count} ${escapeHtml(tr('chapters'))}</span></span><span class="chevron">›</span></button>`).join('')}</div></section>`;}

async function openNovel(id) {
  let novel=[...(state.bootstrap.queue.active||[]),...(state.bootstrap.queue.upcoming||[]),...(state.bootstrap.queue.completed||[]),...(state.bootstrap.my_requests||[])].find(x=>x.id===id);
  if(!novel&&!state.preview){try{novel=(await api(`/api/app/novel/${id}`)).novel;}catch(e){toast(e.message,'error');return;}}
  if(!novel)return;
  state.previousView=state.view; state.view='detail'; renderNav();
  const progress=Number.isFinite(novel.progress_percent)?novel.progress_percent:(novel.current_chapter&&novel.chapter_count?Math.round(novel.current_chapter/novel.chapter_count*100):null);
  const stateName=novel.queue_status==='in_progress'?'in_progress':novel.queue_status==='completed'?'completed':novel.queue_status==='queued'?'queued':novel.status==='rejected'?'rejected':'pending';
  viewRoot.innerHTML=`<section class="page page-back"><button class="back-button" type="button" id="detailBack">‹</button><div class="detail-hero">${cover(novel.title)}<div><div class="detail-title">${escapeHtml(novel.title)}</div><div class="novel-meta" style="margin-top:10px"><span>${languageFlag(novel.original_language)} ${escapeHtml(novel.original_language)}</span><span>→</span><span>English</span></div><div style="margin-top:10px">${statusPill(stateName)}</div>${progress!==null?`<div class="progress-block"><div class="progress-labels"><span>Chapter Progress</span><strong>${novel.current_chapter} / ${novel.chapter_count}</strong></div><div class="progress-track"><div class="progress-fill" style="width:${progress}%"></div></div></div>`:''}</div></div>
  <div class="surface-card info-list"><div class="info-row"><div class="round-icon">▱</div><div><div class="info-label">${escapeHtml(tr('originalLanguage'))}</div><div class="info-value">${escapeHtml(novel.original_language||'—')}</div></div></div><div class="info-row"><div class="round-icon">▤</div><div><div class="info-label">${escapeHtml(tr('totalChapters'))}</div><div class="info-value">${novel.chapter_count} ${escapeHtml(tr('chapters'))}</div></div></div><div class="info-row"><div class="round-icon">#</div><div><div class="info-label">${escapeHtml(tr('queuePosition'))}</div><div class="info-value">${novel.queue_position?`#${novel.queue_position}`:'—'}</div></div></div><div class="info-row"><div class="round-icon">✦</div><div><div class="info-label">${escapeHtml(tr('status'))}</div><div class="info-value">${escapeHtml(tr(stateName==='in_progress'?'inProgress':stateName==='queued'?'inQueue':stateName))}</div></div></div><div class="info-row"><div class="round-icon">◷</div><div><div class="info-label">${escapeHtml(tr('lastUpdated'))}</div><div class="info-value">${escapeHtml(relativeTime(novel.progress_updated_at||novel.updated_at))}</div></div></div></div>
  ${novel.source_url?`<a class="primary-button wide-button" href="${escapeHtml(novel.source_url)}" target="_blank" rel="noopener" style="margin-top:16px">↗ ${escapeHtml(tr('openOriginal'))}</a>`:''}</section>`;
  document.getElementById('detailBack')?.addEventListener('click',()=>navigate(state.previousView||'queue',false));
}

function renderSuggest() {
  if(state.wizardStep===1)return renderUploadStep();
  if(state.wizardStep===2)return renderDetailsStep();
  if(state.wizardStep===3)return renderContentStep();
  return renderReviewStep();
}
function wizardHeader(){const labels=[tr('upload'),tr('details'),tr('tagsContent'),tr('review')];return `<div class="wizard-head"><div class="wizard-title-row"><h1 style="margin:0">${escapeHtml(tr('suggestNovel'))}</h1></div><div class="stepper">${labels.map((label,i)=>{const n=i+1,cls=n<state.wizardStep?'done':n===state.wizardStep?'active':'';return`<div class="step-node ${cls}"><span class="step-circle">${n<state.wizardStep?'✓':n}</span><span>${escapeHtml(label)}</span></div>`}).join('')}</div></div>`;}
function renderUploadStep(){viewRoot.innerHTML=`<section class="page">${wizardHeader()}<div class="upload-zone"><div class="upload-inner"><div class="upload-illustration">⇧</div><h2>${escapeHtml(tr('uploadNovelFile'))}</h2><p>${escapeHtml(tr('uploadCopy'))}</p><button class="primary-button" id="pickFile" type="button">⇧ ${escapeHtml(tr('uploadButton'))}</button><div class="small muted" style="margin-top:12px">◇ ${escapeHtml(tr('filePrivate'))}</div>${state.file?filePickedMarkup():''}</div></div><div class="form-section"><label class="form-label" for="sourceUrl">↗ ${escapeHtml(tr('originalSourceOptional'))}</label><input class="text-input" id="sourceUrl" type="url" value="${escapeHtml(state.draft.source_url)}" placeholder="https://example.com/novel"><p class="form-help">${escapeHtml(tr('sourceHelp'))}</p></div><div class="tip-card" style="margin-top:18px"><div class="round-icon">✦</div><div><div class="tip-title">${escapeHtml(tr('autoFillHint'))}</div><div class="tip-copy">${escapeHtml(tr('autoFillCopy'))}</div></div></div>${state.fileAnalysis?analysisMarkup():''}<button class="primary-button wide-button" id="uploadContinue" type="button" style="margin-top:18px" ${!state.file?'disabled':''}>${escapeHtml(tr('continue'))} ›</button></section>`;
  document.getElementById('pickFile')?.addEventListener('click',()=>filePicker.click());
  document.getElementById('sourceUrl')?.addEventListener('input',e=>state.draft.source_url=e.target.value);
  document.getElementById('uploadContinue')?.addEventListener('click',()=>{state.wizardStep=2;renderSuggest();});
}
function filePickedMarkup(){return `<div class="file-picked"><div class="round-icon">▤</div><div><strong>${escapeHtml(state.file.name)}</strong><div class="small muted">${formatBytes(state.file.size)}</div></div><span class="status-pill green">✓</span></div>`;}
function analysisMarkup(){const a=state.fileAnalysis;return `<div class="analysis-card" style="margin-top:18px"><div class="analysis-head"><div class="analysis-icon">✦</div><div><div class="analysis-title">${escapeHtml(a.done?tr('fileAnalyzed'):tr('analyzing'))}</div><div class="small muted">${a.done?escapeHtml(tr('autoDetected')):''}</div></div></div><div class="analysis-steps">${a.steps.map(step=>`<div class="analysis-step ${step.status}"><span class="step-dot">${step.status==='done'?'✓':'·'}</span>${escapeHtml(step.label)}</div>`).join('')}</div></div>`;}

filePicker.addEventListener('change',async()=>{const file=filePicker.files?.[0];if(!file)return;if(file.size>45*1024*1024){toast(tr('fileTooLarge'),'error');filePicker.value='';return;}const ext=file.name.split('.').pop()?.toLowerCase();if(!['txt','epub'].includes(ext)){toast(tr('unsupportedFile'),'error');filePicker.value='';return;}state.file=file;state.fileAnalysis={done:false,steps:[{label:tr('readingFile'),status:'running'},{label:tr('detectingLanguage'),status:''},{label:tr('findingChapters'),status:''},{label:tr('readingStructure'),status:''}]};renderSuggest();await analyzeFile(file);});

async function analyzeFile(file){try{setAnalysisStep(0,'done');setAnalysisStep(1,'running');let result;if(file.name.toLowerCase().endsWith('.txt'))result=await analyzeTxt(file);else result=await analyzeEpub(file);setAnalysisStep(1,'done');setAnalysisStep(2,'done');setAnalysisStep(3,'done');state.fileAnalysis.done=true;if(result.title&&!state.draft.title)state.draft.title=result.title;if(result.language)state.draft.original_language=result.language;if(result.chapters)state.draft.chapter_count=String(result.chapters);renderSuggest();try{tg?.HapticFeedback?.notificationOccurred('success');}catch{}}catch(e){state.fileAnalysis.done=true;state.fileAnalysis.steps.forEach(s=>{if(s.status==='running')s.status='';});renderSuggest();toast(e.message||tr('genericError'),'error');}}
function setAnalysisStep(index,status){if(!state.fileAnalysis)return;state.fileAnalysis.steps[index].status=status;renderSuggest();}
async function analyzeTxt(file){const text=await file.text();const sample=text.slice(0,800000);const language=detectLanguage(sample);const chapters=detectChapters(sample);const title=detectTxtTitle(sample);return{language,chapters,title};}
async function analyzeEpub(file){const buffer=await file.arrayBuffer();const entries=readZipDirectory(buffer);const names=entries.map(x=>x.name);let metadata={};let textSamples=[];let contentCount=0;for(const entry of entries){if(!/\.(opf|xhtml|html|htm)$/i.test(entry.name))continue;if(entry.uncompressedSize>5_000_000)continue;let text='';try{text=await extractZipText(buffer,entry);}catch{continue;}if(/\.opf$/i.test(entry.name)){metadata=parseOpf(text);continue;}const plain=htmlToText(text);if(plain.length>300){contentCount++;if(textSamples.join('').length<500000)textSamples.push(plain.slice(0,80000));}}
const combined=textSamples.join('\n');const language=normalizeDetectedLanguage(metadata.language)||detectLanguage(combined);const chapters=contentCount||detectChapters(combined);return{language,chapters,title:metadata.title||''};}
function readZipDirectory(buffer){const view=new DataView(buffer);let eocd=-1;for(let i=Math.max(0,buffer.byteLength-65557);i<=buffer.byteLength-22;i++){if(view.getUint32(i,true)===0x06054b50)eocd=i;}if(eocd<0)throw new Error('Could not read EPUB structure.');const count=view.getUint16(eocd+10,true);let offset=view.getUint32(eocd+16,true);const decoder=new TextDecoder();const entries=[];for(let n=0;n<count&&offset+46<=buffer.byteLength;n++){if(view.getUint32(offset,true)!==0x02014b50)break;const method=view.getUint16(offset+10,true),compressedSize=view.getUint32(offset+20,true),uncompressedSize=view.getUint32(offset+24,true),nameLen=view.getUint16(offset+28,true),extraLen=view.getUint16(offset+30,true),commentLen=view.getUint16(offset+32,true),localOffset=view.getUint32(offset+42,true);const name=decoder.decode(new Uint8Array(buffer,offset+46,nameLen));entries.push({name,method,compressedSize,uncompressedSize,localOffset});offset+=46+nameLen+extraLen+commentLen;}return entries;}
async function extractZipText(buffer,entry){const view=new DataView(buffer);if(view.getUint32(entry.localOffset,true)!==0x04034b50)throw new Error('Bad EPUB entry');const nameLen=view.getUint16(entry.localOffset+26,true),extraLen=view.getUint16(entry.localOffset+28,true),start=entry.localOffset+30+nameLen+extraLen;const bytes=new Uint8Array(buffer,start,entry.compressedSize);let out;if(entry.method===0)out=bytes;else if(entry.method===8&&typeof DecompressionStream!=='undefined'){const stream=new Blob([bytes]).stream().pipeThrough(new DecompressionStream('deflate-raw'));out=new Uint8Array(await new Response(stream).arrayBuffer());}else throw new Error('EPUB compression is not supported on this device.');return new TextDecoder('utf-8').decode(out);}
function parseOpf(xml){try{const doc=new DOMParser().parseFromString(xml,'application/xml');const value=names=>{for(const name of names){const el=doc.querySelector(name);if(el?.textContent?.trim())return el.textContent.trim();}return'';};return{title:value(['dc\\:title','title']),language:value(['dc\\:language','language'])};}catch{return{};}}
function htmlToText(html){try{const doc=new DOMParser().parseFromString(html,'text/html');doc.querySelectorAll('script,style,nav').forEach(x=>x.remove());return(doc.body?.textContent||'').replace(/\s+/g,' ').trim();}catch{return html.replace(/<[^>]+>/g,' ');}}
function detectLanguage(text){const hangul=(text.match(/[\uAC00-\uD7AF]/g)||[]).length,kana=(text.match(/[\u3040-\u30FF]/g)||[]).length,han=(text.match(/[\u4E00-\u9FFF]/g)||[]).length,dev=(text.match(/[\u0900-\u097F]/g)||[]).length,cyr=(text.match(/[\u0400-\u04FF]/g)||[]).length;if(hangul>30)return'Korean';if(kana>30)return'Japanese';if(han>50)return'Chinese';if(dev>30)return'Hindi';if(cyr>100)return'Russian';const lower=` ${text.toLowerCase()} `;const scores={English:[' the ',' and ',' of ',' to '],Spanish:[' el ',' la ',' de ',' que '],Portuguese:[' de ',' que ',' não ',' uma '],French:[' le ',' la ',' les ',' des '],German:[' der ',' die ',' und ',' das '],Indonesian:[' yang ',' dan ',' tidak ',' dengan '],Vietnamese:[' và ',' của ',' không ',' một ']};let best='';let score=0;for(const [lang,words]of Object.entries(scores)){const s=words.reduce((sum,w)=>sum+(lower.split(w).length-1),0);if(s>score){score=s;best=lang;}}return score>3?best:'';}
function normalizeDetectedLanguage(value=''){const v=value.toLowerCase();if(v.startsWith('ko'))return'Korean';if(v.startsWith('ja'))return'Japanese';if(v.startsWith('zh'))return'Chinese';if(v.startsWith('en'))return'English';if(v.startsWith('ru'))return'Russian';if(v.startsWith('es'))return'Spanish';if(v.startsWith('pt'))return'Portuguese';if(v.startsWith('fr'))return'French';if(v.startsWith('de'))return'German';if(v.startsWith('id'))return'Indonesian';if(v.startsWith('vi'))return'Vietnamese';if(v.startsWith('hi'))return'Hindi';return value;}
function detectChapters(text){const patterns=[/^(?:chapter|chap\.?|ch\.?)[\s:#.-]*\d+/gim,/^глава[\s:#.-]*\d+/gim,/^제\s*\d+\s*[화장]/gm,/^第\s*\d+\s*[章节話话]/gm];let max=0;for(const re of patterns)max=Math.max(max,(text.match(re)||[]).length);return max||0;}
function detectTxtTitle(text){const lines=text.split(/\r?\n/).map(x=>x.trim()).filter(Boolean).slice(0,15);return lines.find(line=>line.length>2&&line.length<140&&!/^(chapter|chap\.?|глава|제\s*\d+|第\s*\d+)/i.test(line))||'';}
function formatBytes(bytes){if(bytes<1024)return`${bytes} B`;if(bytes<1024*1024)return`${(bytes/1024).toFixed(1)} KB`;return`${(bytes/1024/1024).toFixed(1)} MB`;}

function renderDetailsStep(){const a=state.bootstrap.account;const tooLong=a.plan!=='subscriber'&&Number(state.draft.chapter_count)>a.regular_max_chapters;viewRoot.innerHTML=`<section class="page">${wizardHeader()}${state.fileAnalysis?`<div class="analysis-card"><div class="analysis-head"><div class="analysis-icon">✓</div><div><div class="analysis-title">${escapeHtml(tr('fileAnalyzed'))}</div><div class="small" style="color:var(--green)">✦ ${escapeHtml(tr('autoDetected'))}</div></div></div></div>`:''}<div class="section"><div class="section-header"><h2>${escapeHtml(tr('detectedDetails'))}</h2></div><div class="surface-card detected-list"><div class="detected-row"><div class="round-icon">T</div><div><div class="detected-name">${escapeHtml(tr('novelTitle'))}</div><input class="text-input" id="draftTitle" value="${escapeHtml(state.draft.title)}" style="margin-top:5px"></div></div><div class="detected-row"><div class="round-icon">🌐</div><div><div class="detected-name">${escapeHtml(tr('originalLanguage'))}</div><input class="text-input" id="draftLanguage" value="${escapeHtml(state.draft.original_language)}" style="margin-top:5px"></div></div><div class="detected-row"><div class="round-icon">▤</div><div><div class="detected-name">${escapeHtml(tr('chapterCount'))}</div><input class="text-input" id="draftChapters" type="number" min="1" value="${escapeHtml(state.draft.chapter_count)}" style="margin-top:5px"></div></div><div class="detected-row"><div class="round-icon">◇</div><div><div class="detected-name">${escapeHtml(tr('publicationStatus'))}</div><select class="select-input" id="draftStatus" style="margin-top:5px"><option value="ongoing" ${state.draft.publication_status==='ongoing'?'selected':''}>${escapeHtml(tr('ongoing'))}</option><option value="completed" ${state.draft.publication_status==='completed'?'selected':''}>${escapeHtml(tr('completed'))}</option></select></div></div></div></div><div class="tip-card ${tooLong?'warn':''}" style="margin-top:18px"><div class="round-icon">💡</div><div><div class="tip-title">${escapeHtml(tr('chapterLimit'))}</div><div class="tip-copy">${escapeHtml(a.plan==='subscriber'?tr('boostyNoLimit'):tr('regularLimitCopy'))}</div>${tooLong?`<div class="tip-copy" style="color:var(--red);font-weight:700">${escapeHtml(tr('regularTooLong'))}</div>`:''}</div></div><div class="button-row" style="margin-top:18px"><button class="secondary-button" id="detailsBack">${escapeHtml(tr('back'))}</button><button class="primary-button" id="detailsContinue" ${tooLong?'disabled':''}>${escapeHtml(tr('continue'))} ›</button></div></section>`;
  bindInput('draftTitle','title');bindInput('draftLanguage','original_language');bindInput('draftChapters','chapter_count');bindInput('draftStatus','publication_status');document.getElementById('detailsBack').addEventListener('click',()=>{state.wizardStep=1;renderSuggest();});document.getElementById('detailsContinue').addEventListener('click',()=>{if(!state.draft.title||!state.draft.original_language||!Number(state.draft.chapter_count)){toast('Complete the novel details.','error');return;}state.wizardStep=3;renderSuggest();});}
function bindInput(id,key){document.getElementById(id)?.addEventListener('input',e=>{state.draft[key]=e.target.value;});document.getElementById(id)?.addEventListener('change',e=>{state.draft[key]=e.target.value;});}

function renderContentStep(){const selectedTags=state.draft.genres_tags?state.draft.genres_tags.split(',').map(x=>x.trim()).filter(Boolean):[];viewRoot.innerHTML=`<section class="page">${wizardHeader()}<div class="surface-card" style="padding:16px"><h2>${escapeHtml(tr('genresTags'))}</h2><p class="subtitle" style="margin-top:5px">${escapeHtml(tr('genresHelp'))}</p><div class="tag-list" id="selectedTags" style="margin-top:12px">${selectedTags.map(tag=>`<span class="tag">${escapeHtml(tag)} <button type="button" data-remove-tag="${escapeHtml(tag)}">×</button></span>`).join('')}</div><input class="text-input" id="tagInput" placeholder="${escapeHtml(tr('addTags'))}" style="margin-top:10px"><div class="small muted" style="margin-top:10px;text-transform:uppercase">${escapeHtml(tr('popularTags'))}</div><div class="quick-tags">${POPULAR_TAGS.map(tag=>`<button class="quick-tag" type="button" data-add-tag="${escapeHtml(tag)}">${escapeHtml(tag)} ＋</button>`).join('')}</div></div><div class="surface-card" style="padding:16px;margin-top:14px"><h2>${escapeHtml(tr('sexualContent'))}</h2><p class="subtitle" style="margin-top:5px">${escapeHtml(tr('sexualHelp'))}</p><div class="choice-grid" style="margin-top:12px">${[['none','◇',tr('noSexual')],['suggestive','♢',tr('suggestive')],['explicit','18+',tr('explicit')]].map(([id,icon,label])=>`<button class="choice-card ${state.draft.sexual_level===id?'active':''}" type="button" data-sexual="${id}"><div><div style="font-size:18px;margin-bottom:5px">${icon}</div>${escapeHtml(label)}</div></button>`).join('')}</div>${state.draft.sexual_level!=='none'?`<textarea class="textarea-input" id="sexualDetails" placeholder="${escapeHtml(tr('sexualDetails'))}" style="margin-top:10px">${escapeHtml(state.draft.sexual_content==='None'?'':state.draft.sexual_content)}</textarea>`:''}</div><div class="surface-card" style="padding:16px;margin-top:14px"><h2>${escapeHtml(tr('sensitiveContent'))}</h2><p class="subtitle" style="margin-top:5px">${escapeHtml(tr('sensitiveHelp'))}</p><textarea class="textarea-input" id="sensitiveDetails" style="margin-top:10px">${escapeHtml(state.draft.sensitive_content==='None'?'':state.draft.sensitive_content)}</textarea></div><div class="tip-card" style="margin-top:14px"><div class="round-icon">💡</div><div><div class="tip-title">${escapeHtml(tr('whyMatters'))}</div><div class="tip-copy">${escapeHtml(tr('contentWhy'))}</div></div></div><div class="form-section"><label class="form-label" for="notes">${escapeHtml(tr('additionalNotes'))}</label><textarea class="textarea-input" id="notes" placeholder="${escapeHtml(tr('notesPlaceholder'))}">${escapeHtml(state.draft.notes)}</textarea></div><div class="button-row" style="margin-top:18px"><button class="secondary-button" id="contentBack">${escapeHtml(tr('back'))}</button><button class="primary-button" id="contentContinue">${escapeHtml(tr('continue'))} ›</button></div></section>`;
  document.querySelectorAll('[data-add-tag]').forEach(btn=>btn.addEventListener('click',()=>addTag(btn.dataset.addTag)));document.querySelectorAll('[data-remove-tag]').forEach(btn=>btn.addEventListener('click',()=>removeTag(btn.dataset.removeTag)));document.getElementById('tagInput')?.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===','){e.preventDefault();addTag(e.currentTarget.value);e.currentTarget.value='';}});document.querySelectorAll('[data-sexual]').forEach(btn=>btn.addEventListener('click',()=>{state.draft.sexual_level=btn.dataset.sexual;if(btn.dataset.sexual==='none')state.draft.sexual_content='None';else if(state.draft.sexual_content==='None')state.draft.sexual_content='';renderContentStep();}));document.getElementById('sexualDetails')?.addEventListener('input',e=>state.draft.sexual_content=e.target.value);document.getElementById('sensitiveDetails')?.addEventListener('input',e=>state.draft.sensitive_content=e.target.value||'None');document.getElementById('notes')?.addEventListener('input',e=>state.draft.notes=e.target.value);document.getElementById('contentBack').addEventListener('click',()=>{state.wizardStep=2;renderSuggest();});document.getElementById('contentContinue').addEventListener('click',()=>{if(!state.draft.genres_tags){toast('Add at least one genre or tag.','error');return;}if(state.draft.sexual_level!=='none'&&!state.draft.sexual_content.trim()){toast('Describe the sexual content or fetishes.','error');return;}if(!state.draft.sensitive_content.trim())state.draft.sensitive_content='None';state.wizardStep=4;renderSuggest();});}
function addTag(tag){tag=String(tag||'').trim();if(!tag)return;const tags=state.draft.genres_tags?state.draft.genres_tags.split(',').map(x=>x.trim()).filter(Boolean):[];if(!tags.some(x=>x.toLowerCase()===tag.toLowerCase()))tags.push(tag);state.draft.genres_tags=tags.join(', ');renderContentStep();}
function removeTag(tag){state.draft.genres_tags=(state.draft.genres_tags||'').split(',').map(x=>x.trim()).filter(x=>x&&x!==tag).join(', ');renderContentStep();}

function renderReviewStep(){const a=state.bootstrap.account;const after=Math.min(a.limit,a.used+1);viewRoot.innerHTML=`<section class="page">${wizardHeader()}<div class="page-heading compact"><h2>${escapeHtml(tr('reviewYourRequest'))}</h2><p class="subtitle">${escapeHtml(tr('reviewCopy'))}</p></div><div class="surface-card review-card"><div class="review-row"><div class="round-icon">▤</div><div><div class="review-label">${escapeHtml(tr('uploadedSource'))}</div><div class="review-value">${escapeHtml(state.file?.name||'—')}</div><div class="review-sub">${state.file?formatBytes(state.file.size):''}</div></div><button class="edit-link" data-edit-step="1">Edit</button></div><div class="review-row"><div class="round-icon">▱</div><div><div class="review-label">${escapeHtml(tr('detectedInfo'))}</div><div class="review-value">${escapeHtml(state.draft.title)}</div><div class="review-sub">${languageFlag(state.draft.original_language)} ${escapeHtml(state.draft.original_language)} · ${escapeHtml(state.draft.chapter_count)} ${escapeHtml(tr('chapters'))} · ${escapeHtml(tr(state.draft.publication_status==='completed'?'completed':'ongoing'))}</div></div><button class="edit-link" data-edit-step="2">Edit</button></div><div class="review-row"><div class="round-icon">#</div><div><div class="review-label">${escapeHtml(tr('genresTags'))}</div><div class="review-value">${escapeHtml(state.draft.genres_tags)}</div></div><button class="edit-link" data-edit-step="3">Edit</button></div><div class="review-row"><div class="round-icon">◇</div><div><div class="review-label">${escapeHtml(tr('disclosure'))}</div><div class="review-value">${escapeHtml(state.draft.sexual_content||'None')}</div><div class="review-sub">${escapeHtml(state.draft.sensitive_content||'None')}</div></div><button class="edit-link" data-edit-step="3">Edit</button></div></div><div class="usage-box" style="margin-top:14px"><div class="usage-item"><div class="round-icon">✦</div><div><div class="usage-label">${escapeHtml(tr('afterSubmission'))}</div><div class="usage-value">${after} / ${a.limit}</div><div class="usage-caption">${escapeHtml(tr('requestsUsed'))}</div></div></div><div class="usage-divider"></div><div class="usage-item"><div class="round-icon">▣</div><div><div class="usage-label">${escapeHtml(tr('remainingRequests'))}</div><div class="usage-value">${Math.max(0,a.limit-after)}</div><div class="usage-caption">${escapeHtml(tr('requestsLeft'))}</div></div></div></div><label class="rules-check"><input type="checkbox" id="rulesAccepted" ${state.draft.rules_accepted?'checked':''}><span>${escapeHtml(tr('rulesAgree'))}</span></label><div class="button-row"><button class="secondary-button" id="reviewBack">${escapeHtml(tr('back'))}</button><button class="primary-button" id="submitRequest" ${!state.draft.rules_accepted?'disabled':''}>▱ ${escapeHtml(tr('submitRequest'))}</button></div><div id="submitProgress"></div></section>`;
  document.querySelectorAll('[data-edit-step]').forEach(btn=>btn.addEventListener('click',()=>{state.wizardStep=Number(btn.dataset.editStep);renderSuggest();}));document.getElementById('reviewBack').addEventListener('click',()=>{state.wizardStep=3;renderSuggest();});document.getElementById('rulesAccepted').addEventListener('change',e=>{state.draft.rules_accepted=e.target.checked;renderReviewStep();});document.getElementById('submitRequest')?.addEventListener('click',submitRequest);
}

async function submitRequest(){if(!state.file||!state.draft.rules_accepted)return;if(state.preview){showSubmitted({submission_id:42,used:2,limit:5,remaining:3});return;}const btn=document.getElementById('submitRequest');btn.disabled=true;btn.textContent=tr('submitting');const form=new FormData();form.set('file',state.file,state.file.name);for(const key of ['title','original_language','chapter_count','publication_status','source_url','genres_tags','sexual_content','sensitive_content','notes'])form.set(key,state.draft[key]??'');form.set('rules_accepted','true');try{const data=await api('/api/app/submit',{method:'POST',body:form});showSubmitted(data);await refreshBootstrap(false);try{tg?.HapticFeedback?.notificationOccurred('success');}catch{}}catch(e){btn.disabled=false;btn.textContent=`▱ ${tr('submitRequest')}`;toast(e.message,'error');try{tg?.HapticFeedback?.notificationOccurred('error');}catch{}}}
function showSubmitted(data){viewRoot.innerHTML=`<section class="page"><div class="empty-state surface-card" style="margin-top:40px"><div class="empty-icon" style="background:var(--green-soft);color:var(--green)">✓</div><h1 style="font-size:30px">${escapeHtml(tr('requestSubmitted'))}</h1><p>${escapeHtml(tr('requestSubmittedCopy'))}</p><div class="status-pill gold" style="margin-top:13px">Request #${data.submission_id}</div><button class="primary-button wide-button" id="submittedHome" type="button" style="margin-top:20px">${escapeHtml(tr('backHome'))}</button></div></section>`;document.getElementById('submittedHome').addEventListener('click',()=>{resetDraft();navigate('home');});}
function resetDraft(){state.file=null;state.fileAnalysis=null;filePicker.value='';state.wizardStep=1;state.draft={title:'',original_language:'',chapter_count:'',publication_status:'ongoing',source_url:'',genres_tags:'',sexual_level:'none',sexual_content:'None',sensitive_content:'None',notes:'',rules_accepted:false};}

function renderRequests(){const rows=state.bootstrap.my_requests||[];const filtered=rows.filter(r=>state.requestFilter==='all'||state.requestFilter==='active'&&['pending','queued','in_progress'].includes(r.state)||state.requestFilter==='completed'&&r.state==='completed'||state.requestFilter==='rejected'&&r.state.startsWith('rejected'));viewRoot.innerHTML=`<section class="page"><div class="page-heading"><h1>${escapeHtml(tr('myRequests'))}</h1><p class="subtitle">${escapeHtml(tr('requestsSubtitle'))}</p></div><div class="filter-row">${[['all',tr('all')],['active',tr('active')],['completed',tr('completed')],['rejected',tr('rejected')]].map(([id,label])=>`<button class="filter-chip ${state.requestFilter===id?'active':''}" type="button" data-r-filter="${id}">${escapeHtml(label)}</button>`).join('')}</div><div class="stagger" style="display:grid;gap:12px;margin-top:14px">${filtered.length?filtered.map(requestCard).join(''):emptyCard('▤','No matching requests.')}</div></section>`;document.querySelectorAll('[data-r-filter]').forEach(btn=>btn.addEventListener('click',()=>{state.requestFilter=btn.dataset.rFilter;renderRequests();}));bindNovelLinks();}
function requestCard(r){const steps=requestTimeline(r);return `<button class="surface-card request-card" data-novel="${r.id}" type="button" style="width:100%;text-align:left;border-style:solid">${cover(r.title)}<div><div class="request-top"><div><div class="novel-title">${escapeHtml(r.title)}</div><div class="novel-meta"><span>${languageFlag(r.original_language)} ${escapeHtml(r.original_language)}</span><span>·</span><span>${escapeHtml(formatDate(r.created_at))}</span></div></div>${statusPill(r.state)}</div>${r.state==='queued'&&r.queue_position?`<div class="request-position" style="margin-top:8px">${escapeHtml(tr('position'))} #${r.queue_position}</div>`:''}${r.state==='in_progress'&&Number.isFinite(r.progress_percent)?`<div class="progress-block"><div class="progress-labels"><span>${escapeHtml(tr('inProgress'))}</span><strong>${r.progress_percent}%</strong></div><div class="progress-track"><div class="progress-fill" style="width:${r.progress_percent}%"></div></div></div>`:''}<div class="timeline">${steps.map(s=>`<div class="timeline-step ${s.state}"><span class="timeline-dot">${s.state==='done'?'✓':s.state==='current'?'●':'○'}</span><span>${escapeHtml(s.label)}</span></div>`).join('')}</div></div></button>`;}
function requestTimeline(r){const base=[{label:tr('pending'),state:''},{label:tr('inQueue'),state:''},{label:tr('inProgress'),state:''},{label:tr('completed'),state:''}];if(r.state==='pending')base[0].state='current';else if(r.state==='queued'){base[0].state='done';base[1].state='current';}else if(r.state==='in_progress'){base[0].state='done';base[1].state='done';base[2].state='current';}else if(r.state==='completed')base.forEach(x=>x.state='done');else base[0].state='current';return base;}

function renderAccount(){const b=state.bootstrap,a=b.account;viewRoot.innerHTML=`<section class="page"><div class="page-heading"><h1>${escapeHtml(tr('accountTitle'))}</h1><p class="subtitle">${escapeHtml(tr('accountSubtitle'))}</p></div>${accountCard(a)}<div class="settings-group"><div class="settings-label">${escapeHtml(tr('preferences'))}</div><div class="surface-card settings-list"><button class="setting-row" id="languageSetting" type="button"><span class="round-icon">🌐</span><span><span class="setting-title">${escapeHtml(tr('language'))}</span><span class="setting-sub">${escapeHtml(LANGUAGE_NAMES[state.locale]||state.locale)}</span></span><span class="chevron">›</span></button></div></div><div class="settings-group"><div class="settings-label">${escapeHtml(tr('supportResources'))}</div><div class="surface-card settings-list"><button class="setting-row" id="guideSetting" type="button"><span class="round-icon">▱</span><span><span class="setting-title">${escapeHtml(tr('helpGuide'))}</span><span class="setting-sub">Dollar TL submission flow</span></span><span class="chevron">›</span></button><button class="setting-row" id="rulesSetting" type="button"><span class="round-icon">◇</span><span><span class="setting-title">${escapeHtml(tr('rules'))}</span><span class="setting-sub">Submission and content rules</span></span><span class="chevron">›</span></button><button class="setting-row" id="chatSetting" type="button"><span class="round-icon">✈</span><span><span class="setting-title">${escapeHtml(tr('openTelegramChat'))}</span><span class="setting-sub">@dollartlbot</span></span><span class="chevron">↗</span></button>${a.plan!=='subscriber'?`<button class="setting-row" id="boostySetting" type="button"><span class="round-icon">♛</span><span><span class="setting-title">${escapeHtml(tr('subscription'))}</span><span class="setting-sub">5 requests/month · no 250-chapter restriction</span></span><span class="chevron">↗</span></button>`:''}</div></div></section>`;document.getElementById('languageSetting').addEventListener('click',languageSheet);document.getElementById('guideSetting').addEventListener('click',()=>infoSheet(tr('helpGuide'),'1. Suggest a novel. 2. We review it manually. 3. Accepted novels enter the public queue. 4. You receive Telegram notifications when translation starts or completes.'));document.getElementById('rulesSetting').addEventListener('click',()=>infoSheet(tr('rules'),'Do not hide important tags, sexual/fetish content or sensitive content. Prohibited content remains blocked under the same Dollar TL rules used by the Telegram chat.'));document.getElementById('chatSetting').addEventListener('click',openBotChat);document.getElementById('boostySetting')?.addEventListener('click',()=>openLink(a.boosty_url));}

function languageSheet(){showSheet(`<div class="sheet-title">${escapeHtml(tr('language'))}</div><div class="sheet-actions">${Object.entries(LANGUAGE_NAMES).map(([code,label])=>`<button class="secondary-button wide-button" type="button" data-lang="${code}">${state.locale===code?'✓ ':''}${escapeHtml(label)}</button>`).join('')}</div>`);sheetRoot.querySelectorAll('[data-lang]').forEach(btn=>btn.addEventListener('click',async()=>{const locale=btn.dataset.lang;if(state.preview){state.locale=locale;state.ui={...BASE_UI,...(UI_OVERRIDES[locale]||{})};state.bootstrap.user.locale=locale;closeSheet();renderNav();renderAccount();return;}try{await api('/api/app/language',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({locale})});state.locale=locale;state.bootstrap.user.locale=locale;state.ui={...BASE_UI,...(UI_OVERRIDES[locale]||{})};closeSheet();renderNav();renderAccount();toast('✓','success');}catch(e){toast(e.message,'error');}}));}
function infoSheet(title,copy){showSheet(`<div class="sheet-title">${escapeHtml(title)}</div><div class="sheet-copy">${escapeHtml(copy)}</div><div class="sheet-actions"><button class="primary-button wide-button" type="button" data-close-sheet>${escapeHtml(tr('close'))}</button></div>`);}
function showSheet(content){sheetRoot.innerHTML=`<div class="sheet-backdrop" id="sheetBackdrop"><div class="bottom-sheet" role="dialog" aria-modal="true"><div class="sheet-handle"></div>${content}</div></div>`;document.getElementById('sheetBackdrop').addEventListener('click',e=>{if(e.target.id==='sheetBackdrop')closeSheet();});sheetRoot.querySelectorAll('[data-close-sheet]').forEach(x=>x.addEventListener('click',closeSheet));}
function closeSheet(){sheetRoot.innerHTML='';}
function openLink(url){if(!url)return;try{tg?.openLink(url);}catch{window.open(url,'_blank','noopener');}}
function openBotChat(){const url='https://t.me/dollartlbot';try{tg?.openTelegramLink(url);}catch{location.href=url;}}

async function renderAdmin(){if(!state.bootstrap.user.is_admin){navigate('home');return;}viewRoot.innerHTML=`<section class="page"><div class="page-heading"><h1>${escapeHtml(tr('adminTitle'))}</h1><p class="subtitle">${escapeHtml(tr('adminSubtitle'))}</p></div>${adminStats(state.bootstrap.admin)}<div class="filter-row" style="margin-top:20px">${[['pending',tr('pendingRequests')],['queue',tr('queue')],['all',tr('all')]].map(([id,label])=>`<button class="filter-chip ${state.adminKind===id?'active':''}" type="button" data-admin-kind="${id}">${escapeHtml(label)}</button>`).join('')}</div><div id="adminList" style="margin-top:13px">${loadingCards()}</div></section>`;document.querySelectorAll('[data-admin-kind]').forEach(btn=>btn.addEventListener('click',()=>{state.adminKind=btn.dataset.adminKind;renderAdmin();}));await loadAdminList();}
function adminStats(c={}){return `<div class="admin-stats"><div class="stat-card"><div class="stat-icon">◷</div><div class="stat-value">${c.pending??0}</div><div class="stat-label">${escapeHtml(tr('pending'))}</div></div><div class="stat-card"><div class="stat-icon">▱</div><div class="stat-value">${c.queued??0}</div><div class="stat-label">${escapeHtml(tr('queued'))}</div></div><div class="stat-card"><div class="stat-icon">✦</div><div class="stat-value">${c.in_progress??0}</div><div class="stat-label">${escapeHtml(tr('adminInProgress'))}</div></div><div class="stat-card"><div class="stat-icon">✓</div><div class="stat-value">${c.completed??0}</div><div class="stat-label">${escapeHtml(tr('adminCompleted'))}</div></div></div>`;}
function loadingCards(){return'<div class="skeleton skeleton-card"></div><div class="skeleton skeleton-card" style="margin-top:10px"></div>';}
async function loadAdminList(){if(state.preview){state.adminData={counts:state.bootstrap.admin,requests:previewAdminRows(state.adminKind)};renderAdminRows();return;}try{state.adminData=await api(`/api/app/admin/list?kind=${encodeURIComponent(state.adminKind)}`);state.bootstrap.admin=state.adminData.counts;renderAdminRows();}catch(e){document.getElementById('adminList').innerHTML=emptyCard('!',e.message);}}
function previewAdminRows(kind){const all=[{id:31,title:'Some Korean Novel',original_language:'Korean',chapter_count:212,publication_status:'ongoing',plan:'subscriber',status:'pending',queue_status:null,created_at:new Date(Date.now()-1500000).toISOString(),username:'alex'},{id:2,title:'Pokémon Master of Tactics',original_language:'Korean',chapter_count:196,publication_status:'ongoing',plan:'subscriber',status:'accepted',queue_status:'queued',queue_position:2,created_at:new Date().toISOString(),username:'maria'},{id:1,title:'The Pure Love of the Humiliation Academy',original_language:'Korean',chapter_count:258,publication_status:'ongoing',plan:'subscriber',status:'accepted',queue_status:'in_progress',current_chapter:78,created_at:new Date().toISOString(),username:'reader'}];if(kind==='pending')return all.filter(x=>x.status==='pending');if(kind==='queue')return all.filter(x=>x.status==='accepted');return all;}
function renderAdminRows(){const el=document.getElementById('adminList');if(!el)return;const rows=state.adminData?.requests||[];if(!rows.length){el.innerHTML=emptyCard('✓','Nothing here.');return;}el.innerHTML=`<div class="stagger" style="display:grid;gap:10px">${rows.map((r,i)=>adminRequestCard(r,i===0)).join('')}</div>`;el.querySelectorAll('[data-admin-action]').forEach(btn=>btn.addEventListener('click',()=>confirmAdminAction(btn.dataset.adminAction,Number(btn.dataset.id))));el.querySelectorAll('[data-progress-save]').forEach(btn=>btn.addEventListener('click',()=>saveProgress(Number(btn.dataset.progressSave))));}
function adminRequestCard(r,featured){const isPending=r.status==='pending',isQueued=r.status==='accepted'&&r.queue_status==='queued',isProgress=r.status==='accepted'&&r.queue_status==='in_progress';return `<div class="admin-request ${featured?'featured':''}"><div class="admin-request-head"><div><div class="eyebrow">${featured?escapeHtml(tr('requiresAction')):`#${r.id}`}</div><div class="card-title">${escapeHtml(r.title)}</div><div class="admin-meta">${languageFlag(r.original_language)} ${escapeHtml(r.original_language)} · ${r.chapter_count} ${escapeHtml(tr('chapters'))}<br>${r.plan==='subscriber'?'⭐ Boosty · ':''}${r.username?'@'+escapeHtml(r.username)+' · ':''}${escapeHtml(relativeTime(r.created_at))}</div></div>${statusPill(isPending?'pending':isProgress?'in_progress':isQueued?'queued':r.queue_status==='completed'?'completed':'rejected')}</div>${isPending?`<div class="admin-actions"><button class="success-button" data-admin-action="accept" data-id="${r.id}">✓ ${escapeHtml(tr('accept'))}</button><button class="danger-button" data-admin-action="reject" data-id="${r.id}">× ${escapeHtml(tr('reject'))}</button><button class="secondary-button" data-admin-action="return" data-id="${r.id}">↩ ${escapeHtml(tr('returnSlot'))}</button></div>`:''}${isQueued?`<div class="admin-actions"><button class="success-button" data-admin-action="start" data-id="${r.id}">▶ ${escapeHtml(tr('start'))}</button><button class="secondary-button" data-admin-action="up" data-id="${r.id}">↑ ${escapeHtml(tr('moveUp'))}</button><button class="secondary-button" data-admin-action="down" data-id="${r.id}">↓ ${escapeHtml(tr('moveDown'))}</button></div>`:''}${isProgress?`<div class="progress-editor"><input class="text-input" id="progress-${r.id}" type="number" min="0" max="${r.chapter_count}" value="${r.current_chapter??0}" aria-label="${escapeHtml(tr('currentChapter'))}"><button class="secondary-button" data-progress-save="${r.id}">${escapeHtml(tr('update'))}</button></div><div class="admin-actions"><button class="success-button" data-admin-action="complete" data-id="${r.id}">✓ ${escapeHtml(tr('complete'))}</button><button class="secondary-button" data-admin-action="backqueue" data-id="${r.id}">↩ ${escapeHtml(tr('backToQueue'))}</button></div>`:''}</div>`;}
function confirmAdminAction(action,id){const destructive=['reject','return','complete'].includes(action);const labels={accept:tr('accept'),reject:tr('reject'),return:tr('returnSlot'),start:tr('start'),complete:tr('complete'),backqueue:tr('backToQueue'),up:tr('moveUp'),down:tr('moveDown')};if(['up','down','start','backqueue'].includes(action)){runAdminAction(action,id);return;}showSheet(`<div class="sheet-title">${escapeHtml(labels[action]||action)}?</div><div class="sheet-copy">Request #${id}</div><div class="sheet-actions"><button class="${destructive?'danger-button':'success-button'} wide-button" type="button" id="confirmAdmin">${escapeHtml(tr('confirm'))}</button><button class="secondary-button wide-button" type="button" data-close-sheet>${escapeHtml(tr('cancel'))}</button></div>`);document.getElementById('confirmAdmin').addEventListener('click',()=>{closeSheet();runAdminAction(action,id);});}
async function runAdminAction(action,id,extra={}){if(state.preview){toast('Preview: action simulated.','success');return;}try{const data=await api('/api/app/admin/action',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({id,action,...extra})});state.bootstrap.admin=data.counts;await refreshBootstrap(false);renderAdmin();toast('✓','success');try{tg?.HapticFeedback?.notificationOccurred('success');}catch{}}catch(e){toast(e.message,'error');}}
function saveProgress(id){const value=Number(document.getElementById(`progress-${id}`)?.value);runAdminAction('progress',id,{current_chapter:value});}

async function refreshBootstrap(render=true){if(state.preview)return;state.bootstrap=await api('/api/app/bootstrap');state.locale=state.bootstrap.user.locale||state.locale;state.ui={...BASE_UI,...(UI_OVERRIDES[state.locale]||{})};renderNav();if(render)renderView();}
function toast(message,type=''){const el=document.createElement('div');el.className=`toast ${type}`;el.textContent=message;toastRegion.appendChild(el);setTimeout(()=>{el.style.opacity='0';el.style.transform='translateY(6px)';setTimeout(()=>el.remove(),180);},3200);}

root.addEventListener('click',event=>{const nav=event.target.closest?.('[data-nav]');if(nav&&nav.closest('.brand'))navigate(nav.dataset.nav);});
document.getElementById('notificationButton').addEventListener('click',()=>toast('Telegram bot notifications are enabled for request status updates.'));

init();
