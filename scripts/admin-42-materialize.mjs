import fs from 'node:fs';

function replaceOne(source, pattern, replacement, label) {
  const matches = source.match(pattern);
  if (!matches || matches.length !== 1) throw new Error(`${label}: expected one match, got ${matches?.length ?? 0}`);
  return source.replace(pattern, replacement);
}

let ops = fs.readFileSync('src/admin-request-ops.ts', 'utf8');
const moveReplacement = "async function moveToPosition(request:Request,id:number,adminId:number,env:Env):Promise<Response>{\n  const body=await readJson<{position?:number}>(request);\n  const desired=Number(body.position);\n  const [current,count]=await Promise.all([\n    env.DB.prepare(`SELECT queue_position FROM submissions WHERE id=? AND status='accepted' AND queue_status='queued'`)\n      .bind(id).first<{queue_position:number|null}>(),\n    env.DB.prepare(`SELECT COUNT(*) AS n FROM submissions WHERE status='accepted' AND queue_status='queued'`).first<{n:number}>(),\n  ]);\n  if(!current?.queue_position)return miniAppJsonError('invalid_state','\u041f\u043e\u0437\u0438\u0446\u0438\u044e \u043c\u043e\u0436\u043d\u043e \u043c\u0435\u043d\u044f\u0442\u044c \u0442\u043e\u043b\u044c\u043a\u043e \u0443 \u0437\u0430\u044f\u0432\u043a\u0438 \u0432 \u043e\u0447\u0435\u0440\u0435\u0434\u0438.',409);\n  const max=Math.max(1,Number(count?.n||0));\n  if(!Number.isInteger(desired)||desired<1||desired>max)return miniAppJsonError('invalid_position',`\u041f\u043e\u0437\u0438\u0446\u0438\u044f \u0434\u043e\u043b\u0436\u043d\u0430 \u0431\u044b\u0442\u044c \u043e\u0442 1 \u0434\u043e ${max}.`,400);\n  if(desired===current.queue_position)return requestDetail(id,env);\n\n  const now=new Date().toISOString();\n  const moved=await env.DB.prepare(`\n    WITH ordered AS (\n      SELECT id,ROW_NUMBER() OVER (ORDER BY COALESCE(queue_position,2147483647),id) AS rn\n      FROM submissions\n      WHERE status='accepted' AND queue_status='queued' AND id<>?\n    ), desired_order AS (\n      SELECT id,CASE WHEN rn>=? THEN rn+1 ELSE rn END AS next_position FROM ordered\n      UNION ALL SELECT ?,?\n    )\n    UPDATE submissions\n    SET queue_position=(SELECT next_position FROM desired_order WHERE desired_order.id=submissions.id),\n        updated_at=?\n    WHERE status='accepted' AND queue_status='queued'\n      AND id IN (SELECT id FROM desired_order)\n      AND EXISTS (\n        SELECT 1 FROM submissions target\n        WHERE target.id=? AND target.status='accepted' AND target.queue_status='queued'\n      )\n      AND (SELECT COUNT(*) FROM submissions q WHERE q.status='accepted' AND q.queue_status='queued')>=?\n  `).bind(id,desired,id,desired,now,id,desired).run();\n  if(Number(moved.meta.changes??0)<1)return miniAppJsonError('stale_state','\u041e\u0447\u0435\u0440\u0435\u0434\u044c \u0438\u0437\u043c\u0435\u043d\u0438\u043b\u0430\u0441\u044c. \u041f\u043e\u0432\u0442\u043e\u0440\u0438\u0442\u0435 \u0434\u0435\u0439\u0441\u0442\u0432\u0438\u0435.',409);\n\n  const after=await env.DB.prepare(`SELECT queue_position FROM submissions WHERE id=? AND status='accepted' AND queue_status='queued'`)\n    .bind(id).first<{queue_position:number|null}>();\n  if(Number(after?.queue_position)!==desired){\n    await normalizeQueuePositions(env);\n    return miniAppJsonError('stale_state','\u041e\u0447\u0435\u0440\u0435\u0434\u044c \u0438\u0437\u043c\u0435\u043d\u0438\u043b\u0430\u0441\u044c \u0432\u043e \u0432\u0440\u0435\u043c\u044f \u043f\u0435\u0440\u0435\u0441\u0442\u0430\u043d\u043e\u0432\u043a\u0438. \u041e\u0431\u043d\u043e\u0432\u0438\u0442\u0435 \u044d\u043a\u0440\u0430\u043d.',409);\n  }\n  await audit(env,adminId,'submission_queue_position',id,{before:current.queue_position,requested:desired,after:desired,atomic:true});\n  return requestDetail(id,env);\n}";
ops = replaceOne(ops, /async function moveToPosition\([\s\S]*?\n}\n\n(?=async function saveMeta\()/g, moveReplacement + '\n\n', 'moveToPosition');
if (!ops.includes('atomic:true') || !ops.includes('WITH ordered AS (')) throw new Error('atomic queue reorder patch missing invariants');
fs.writeFileSync('src/admin-request-ops.ts', ops);

let core = fs.readFileSync('src/miniapp-core.ts', 'utf8');
core = replaceOne(core, /const kind = url\.searchParams\.get\('kind'\) \?\? 'pending';\n      return miniAppJson\(await getAdminList\(env, kind\)\);/g, "return miniAppJson(await getAdminList(env, url));", 'admin list route');
const adminReplacement = "async function getAdminList(env: Env, url: URL) {\n  const kind = (url.searchParams.get('kind') ?? 'pending').trim();\n  if (!['pending', 'active', 'completed', 'rejected', 'all', 'queue'].includes(kind)) {\n    throw new Error('invalid_admin_list_filter');\n  }\n\n  const counts = await getAdminCounts(env);\n  if (kind === 'queue') {\n    const limit = 500;\n    const [rows, count] = await Promise.all([\n      env.DB.prepare(`\n        SELECT s.id, s.user_id, s.title, s.original_language, s.chapter_count,\n               s.publication_status, s.source_url, s.genres_tags, s.sexual_content,\n               s.sensitive_content, s.notes, s.plan, s.status, s.slot_returned,\n               s.queue_status, s.queue_position, s.current_chapter, s.progress_updated_at,\n               s.created_at, s.updated_at, u.username, u.first_name\n        FROM submissions s\n        LEFT JOIN users u ON u.telegram_id = s.user_id\n        WHERE s.status = 'accepted' AND s.queue_status IN ('queued', 'in_progress')\n        ORDER BY CASE WHEN s.queue_status = 'in_progress' THEN 0 ELSE 1 END,\n                 COALESCE(s.queue_position, 2147483647), s.id\n        LIMIT ?\n      `).bind(limit).all<Record<string, unknown>>(),\n      env.DB.prepare(\"SELECT COUNT(*) AS n FROM submissions WHERE status='accepted' AND queue_status IN ('queued','in_progress')\")\n        .first<{ n: number }>(),\n    ]);\n    const total = Number(count?.n ?? 0);\n    return {\n      counts,\n      requests: rows.results,\n      page: { total, limit, next_cursor: null, has_more: total > rows.results.length, truncated: total > rows.results.length },\n    };\n  }\n\n  const limit = boundedInt(url.searchParams.get('limit'), 30, 1, 50);\n  const cursor = positiveInt(url.searchParams.get('cursor'));\n  const q = (url.searchParams.get('q') ?? '').trim().slice(0, 120).toLowerCase();\n  const where = kind === 'pending'\n    ? \"s.status='pending'\"\n    : kind === 'active'\n      ? \"s.status='accepted' AND s.queue_status IN ('queued','in_progress')\"\n      : kind === 'completed'\n        ? \"s.status='accepted' AND s.queue_status='completed'\"\n        : kind === 'rejected'\n          ? \"s.status='rejected'\"\n          : '1=1';\n  const search = q ? ` AND (\n    CAST(s.id AS TEXT) LIKE ? ESCAPE '!'\n    OR CAST(s.user_id AS TEXT) LIKE ? ESCAPE '!'\n    OR LOWER(s.title) LIKE ? ESCAPE '!'\n    OR LOWER(COALESCE(u.username,'')) LIKE ? ESCAPE '!'\n    OR LOWER(COALESCE(u.first_name,'')) LIKE ? ESCAPE '!'\n    OR LOWER(s.original_language) LIKE ? ESCAPE '!'\n  )` : '';\n  const cursorClause = cursor ? ' AND s.id < ?' : '';\n  const needle = q ? `%${escapeLike(q)}%` : '';\n  const commonBinds = q ? [needle, needle, needle, needle, needle, needle] : [];\n  const rowBinds = cursor ? [...commonBinds, cursor, limit + 1] : [...commonBinds, limit + 1];\n\n  const [rows, count] = await Promise.all([\n    env.DB.prepare(`\n      SELECT s.id, s.user_id, s.title, s.original_language, s.chapter_count,\n             s.publication_status, s.source_url, s.genres_tags, s.sexual_content,\n             s.sensitive_content, s.notes, s.plan, s.status, s.slot_returned,\n             s.queue_status, s.queue_position, s.current_chapter, s.progress_updated_at,\n             s.created_at, s.updated_at, u.username, u.first_name\n      FROM submissions s\n      LEFT JOIN users u ON u.telegram_id = s.user_id\n      WHERE ${where}${search}${cursorClause}\n      ORDER BY s.id DESC\n      LIMIT ?\n    `).bind(...rowBinds).all<Record<string, unknown>>(),\n    env.DB.prepare(`\n      SELECT COUNT(*) AS n\n      FROM submissions s\n      LEFT JOIN users u ON u.telegram_id = s.user_id\n      WHERE ${where}${search}\n    `).bind(...commonBinds).first<{ n: number }>(),\n  ]);\n\n  const hasMore = rows.results.length > limit;\n  const visible = hasMore ? rows.results.slice(0, limit) : rows.results;\n  const nextCursor = hasMore && visible.length ? Number(visible[visible.length - 1]?.id ?? 0) : null;\n  return {\n    counts,\n    requests: visible,\n    page: { total: Number(count?.n ?? 0), limit, next_cursor: nextCursor || null, has_more: hasMore, query: q, kind },\n  };\n}\n\nfunction boundedInt(raw: string | null, fallback: number, min: number, max: number): number {\n  const value = Number(raw);\n  return raw && Number.isInteger(value) ? Math.max(min, Math.min(max, value)) : fallback;\n}\nfunction positiveInt(raw: string | null): number | null {\n  const value = Number(raw);\n  return raw && Number.isSafeInteger(value) && value > 0 ? value : null;\n}\nfunction escapeLike(value: string): string {\n  return value.replace(/!/g, '!!').replace(/%/g, '!%').replace(/_/g, '!_');\n}";
core = replaceOne(core, /async function getAdminList\(env: Env, kind: string\) \{[\s\S]*?\n}\n\n(?=async function readJson<)/g, adminReplacement + '\n\n', 'getAdminList');
for (const token of ["kind === 'active'", "kind === 'completed'", "kind === 'rejected'", 'next_cursor', "LIKE ? ESCAPE '!'"]) {
  if (!core.includes(token)) throw new Error(`admin list patch missing ${token}`);
}
fs.writeFileSync('src/miniapp-core.ts', core);
console.log('Admin 4.2 backend patch applied.');
